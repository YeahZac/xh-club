---
description: 后端（尤其登录鉴权）改动完成后主动推送到 GitHub
globs: server/**/*,deploy/**/*
alwaysApply: true
---

# 后端改动主动推送 GitHub

用户已明确要求：涉及后端改动（尤其是**登录 / 鉴权 / 微信授权 / callContainer 相关后端**）完成后，**主动**提交并推送到 GitHub，无需再询问是否推送。

## 触发范围

包括但不限于：

- `server/src/auth/**`
- `server/src/members/**`（会员资料、会话相关）
- `server/src/upload/**`（登录后头像等上传桥接）
- 其它 `server/**`、`deploy/**` 中为登录链路服务的改动

## 执行步骤

1. 只 `git add` 本次后端相关安全文件（不要提交含 `WX_APP_SECRET`、`.env`、明文密钥的配置）
2. 用 Commitlint 规范提交：`feat:` / `fix:` 等
3. `git push` 到当前跟踪远程分支（通常 `origin/main`）
4. **禁止** force push
5. 推送成功后回报 commit hash，并提醒用户云托管重新部署（若走 Git 构建）

## 安全

- 根目录 `container.config.json` 若含 AppSecret，不要提交
- 密钥只放云托管控制台环境变量
